<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.14">
  <compounddef id="install" kind="page">
    <compoundname>install</compoundname>
    <title>Installation</title>
    <briefdescription>
    </briefdescription>
    <detaileddescription>
<para><heading level="1">libFMS Installation</heading>
</para><para><heading level="2">External Requirements</heading>
</para><para>The following external libraries are required when building libFMS</para><para><itemizedlist>
<listitem><para>NetCDF C and Fortran (77/90) headers and libraries</para></listitem><listitem><para>Fortran 2003 standard compiler</para></listitem><listitem><para>Fortran compiler that supports Cray Pointer</para></listitem><listitem><para>MPI C and Fortran headers and libraries (optional)</para></listitem><listitem><para>Libyaml header and libraries (optional)</para></listitem><listitem><para>Linux or Unix style system</para></listitem></itemizedlist>
</para><para><heading level="2">Supported Compilers</heading>
</para><para>We strive to have libFMS built with as many C and Fortran compilers on as many Unix/Linux type systems as possible. However, internally, we only use libFMS compiled with the Intel compilers. Some groups have had success using libFMS when compiled with the GNU C and Fortran compilers.</para><para><heading level="2">MPI Support</heading>
</para><para>The default way to build libFMS is with MPI support. We have found that using the MPI aware compiler, or the MPI compiler wrappers (mpif90, mpicc, etc.), in general, offer the best results when building with MPI support. If you decide to not use an MPI aware compiler, you should pass the include and library locations to the build system.</para><para>libFMS can be built without MPI support (sometimes called &quot;no-comm mode&quot;). To build libFMS without MPI support, pass to <computeroutput>configure</computeroutput> the <computeroutput>--with-mpi=no</computeroutput> flag.</para><para><heading level="2">Supported Build Systems</heading>
</para><para>The FMS repository has two build systems in place, GNU autotools and CMake. It is also compiled with the GFDL&apos;s internally-developed build tool, <ulink url="https://github.com/noaa-gfdl/mkmf">mkmf</ulink></para><para>CMake and Autotools have some variation in options and the resulting build targets.</para><para>Autotools will build with 64 bit real defaults unless configured with <computeroutput>--enable-mixed-mode</computeroutput> in which case it will default to 32 bit reals and add overloads to allow for both 32 bit and 64 bit operations. This results in one library with both 32 bit and 64 bit routines (defaulting to 32 bit reals).</para><para>By default CMake will build the library with 32 bit reals. It also has an option for 64 bit defaults , and if enabled will build an additional library with 64 bit reals. This results in distinct 32 bit and 64 bit libraries (libfms_r4.a and libfms_r8.a)</para><para><heading level="3">GNU Autoconf/Automake</heading>
</para><para>In many cases, running the shell command <computeroutput>./configure &amp;&amp; make &amp;&amp; make install</computeroutput> will build and install this package. Since libFMS requires the netCDF libraries, you will likely need to tell configure where to find the C and Fortran netCDF headers and libraries via the <computeroutput>CPPFLAGS</computeroutput>, <computeroutput>FCFLAGS</computeroutput> and <computeroutput>LDFLAGS</computeroutput> variables</para><para><programlisting filename=".bash"><codeline><highlight class="normal">&gt;<sp/>./configure<sp/>CPPFLAGS=&quot;-I/path/to/netcdf/include&quot;<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>FCFLAGS=&quot;-I/path/to/netcdff/include&quot;<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>LDFLAGS=&quot;-L/path/to/netcdf/lib<sp/>-L/path/to/netcdff/lib&quot;</highlight></codeline>
</programlisting></para><para>When building from a GitHub clone, the user must run <computeroutput>autoreconf -i</computeroutput> before running the <computeroutput>./configure</computeroutput> script.</para><para>The <computeroutput>./configure</computeroutput> script will guess as many compiler options as required to build libFMS. In some cases you may be required to supply additional compiler flags for your system. Additional compiler options can be passed to the build with the <computeroutput>CPPFLAGS</computeroutput>, <computeroutput>CFLAGS</computeroutput> and <computeroutput>FCFLAGS</computeroutput> variable. The <computeroutput>./configure</computeroutput> option <computeroutput>--disable-setting-flags</computeroutput> will not guess required compiler flags. Using this configure option will require the user to give all required flags.</para><para>Run <computeroutput>./configure --help</computeroutput> to see other available configure options.</para><para>For more information on building with autotools and build options please see https://github.com/NOAA-GFDL/FMS/blob/main/libFMS/BUILD_SYSTEM.md &quot;BUILD_SYSTEM.md&quot;</para><para><heading level="3">CMake</heading>
</para><para>To build using CMake, follow the instructions in https://github.com/NOAA-GFDL/FMS/blob/main/CMAKE_INSTRUCTIONS.md &quot;CMAKE_INSTRUCTIONS.mk&quot;. Currently the CMake configuration is the most restrictive build option as the compiler flags are immutable.</para><para><heading level="3">GFDL MKMF</heading>
</para><para>Make Makefile (<ulink url="https://github.com/NOAA-GFDL/mkmf">MKMF</ulink>) is the GFDL developed makefile generator application. MKMF uses a list of file, or a list of directories that contain Fortran or C file, a Make template file, and writes a Makefile with the correct dependencies to build a library or executable. To use MKMF to build libFMS, do the following.</para><para><orderedlist>
<listitem><para>Clone the MKMF repository from GitHub <computeroutput>git clone <ulink url="https://github.com/NOAA-GFDL/mkmf.git">https://github.com/NOAA-GFDL/mkmf.git</ulink></computeroutput></para></listitem><listitem><para>Add the <computeroutput>mkmf/bin</computeroutput> directory to your PATH <computeroutput>export PATH=/path/to/mkmf/bin:${PATH}</computeroutput> or <computeroutput>setenv PATH /path/to/mkmf/bin:${PATH}</computeroutput></para></listitem><listitem><para>Run the <computeroutput>list_paths</computeroutput> command, on the FMS directory <computeroutput>list_paths path/to/FMS</computeroutput> This will create a path_names file in the current directory</para></listitem><listitem><para>Pick a MKMF template from the <computeroutput>mkmf/templates</computeroutput> directory that most closely matches you system and compiler. Make any modifications as need</para></listitem><listitem><para>Run <computeroutput>mkmf</computeroutput> <programlisting filename=".shell"><codeline><highlight class="normal">mkmf<sp/>-c<sp/>&quot;-Duse_netCDF<sp/>-Duse_libMPI&quot;<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/>-I<sp/>/path/to/netcdff/include<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/>-I<sp/>/path/to/netcdf/include<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/>-t<sp/>/path/to/mkmf/template.<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/>-p<sp/>libfms.a<sp/>\</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/>path_names</highlight></codeline>
</programlisting></para></listitem><listitem><para>Once built, place the <computeroutput>libfms.a</computeroutput>, <computeroutput>*.mod</computeroutput>, <computeroutput>include/\*</computeroutput> files in your install location.</para></listitem></orderedlist>
</para><para>While MKMF has been the build system used by GFDL to build all the GFDL models for some time, this build method requires the most user intervention when building libFMS on a new system. </para>    </detaileddescription>
  </compounddef>
</doxygen>
